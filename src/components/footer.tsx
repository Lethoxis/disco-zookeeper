import { Region } from "@/assets/regions";
import { useEffect, useState } from "react";

type Props = {};

export default function Footer({}: Props) {
  return (
    <div className="relative flex justify-between items-center w-full h-10 line-shadow shadow-sm text-white px-4 mt-auto bg-[#CF7110] border-t-2 border-[#FFA422]"></div>
  );
}
